import './styles.scss'

export default function Nav () {
    return (
        <nav>
            <button>Dark/Light Mode</button>
            <a href="https://example.com">My Catalog</a>
        </nav>
    )
}